{
  "name": "house-rent-backend",
  "version": "1.0.0",
  "description": "House Rent Management System Backend API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "postinstall": "cd ../frontend && npm install && npm run build && cd ../backend && rm -rf public && cp -r ../frontend/build ./public || true"
  },
  "keywords": [
    "mern",
    "react",
    "node",
    "mongodb",
    "house-rental"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.0.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express-validator": "^7.0.0",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
